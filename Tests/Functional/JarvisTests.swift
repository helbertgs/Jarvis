import Nimble
import Quick
import XCTest
@testable import Jarvis

final class JarvisTests: QuickSpec {
    override func spec() {
        describe("") {
            context("") {
                it("") {
                    expect(0).to(equal(0))
                }
            }
        }
    }

    static var allTests = [
        ("spec", spec),
    ]
}

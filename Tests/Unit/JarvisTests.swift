import Nimble
import Quick
import XCTest
@testable import Jarvis

final class JarvisTests: QuickSpec {
    override func spec() {
        describe("Jaris") {
            context("when init") {
                it("") {
                    let jarvis = Jarvis(url: "")
                }
            }
        }
    }

    static var allTests = [
        ("spec", spec),
    ]
}
